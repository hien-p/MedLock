# ---- Sui / Seal ----
# testnet | mainnet | localnet
SUI_NETWORK=testnet

# Your Move package that defines seal_approve* (hex 0x...)
SEAL_PACKAGE_ID=0xYOUR_PACKAGE_ID

# Optional "policy id" that your seal_approve expects (hex without the package prefix).
# If your policy ignores it, you can leave as 00 (empty vector)
SEAL_POLICY_ID=00

# Comma-separated list of Seal key server object IDs (onchain objects), e.g.:
# 0x73d05d62c18d9374e3ea529e8e0ed6161da1a141a94d3f76ae3fe4e99356db75,0xf5d14a81a982144ae441cd7d64b09027f116a468bd36e7eca494f750591623c8
SEAL_KEY_SERVERS=0xKEYSERVER_OBJ_ID_1,0xKEYSERVER_OBJ_ID_2

# How many key servers must respond (≤ number of listed servers)
SEAL_THRESHOLD=2

# Verify that listed key servers actually host the claimed onchain object id (adds network roundtrips).
SEAL_VERIFY_KEY_SERVERS=false

# ---- Walrus ----
# Number of epochs to store the blob for (Testnet epoch ≈ 1 day; Mainnet epoch ≈ 14 days)
WALRUS_EPOCHS=2

# Optional: if your Walrus CLI supports a --context flag, set it here (e.g., testnet or mainnet).
WALRUS_CONTEXT=testnet

# Optional: override Sui RPC used by walrus CLI (otherwise it uses your walrus/sui wallet config)
# WALRUS_RPC_URL=https://fullnode.testnet.sui.io:443

# ---- Enclave ----
# Where your Nautilus/Nitro enclave is exposed (parent instance forwards to the enclave):
ENCLAVE_URL=http://localhost:3000
